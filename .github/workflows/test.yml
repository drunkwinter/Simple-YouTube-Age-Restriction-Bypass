name: Example Workflow

on:
  workflow_dispatch:

jobs:
  say-hello:
    name: Say Hello
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - name: Print to Log
        id: print-to-log
        uses: ./.github/workflows/test
